- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install gh
    package:
      name: gh

  - name: gitconfig
    copy:
      src: gitconfig
      dest: $HOME/.gitconfig
      remote_src: yes
